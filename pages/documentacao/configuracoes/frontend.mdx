import { Callout, Steps } from "nextra/components";

# Frontend 💻

O frontend necessita do [backend](backend) para funcionar corretamente, certifique-se de tê-lo previamente configurado, juntamente do [banco de dados](database).

<Callout type="info" emoji="ℹ️">
  É essencial que o [Node](https://nodejs.org) já esteja instalado em sua
  máquina.
</Callout>

## Passo a passo

Para configurar o frontend na sua máquina, siga os passos abaixo:

<Steps>

### Criar um fork no GitHub

### Clonar o repositório

### Instalar as dependências

Dentro da pasta clonada em sua máquina, execute o comando abaixo:

```shell copy filename="Terminal"
npm install
```

### Configurar a URL do backend

Verifique se a variável da URL do backend está correta no arquivo a seguir:

```ts {4} showLineNumbers filename="src/app/services/http.ts"
import axios from "axios";

const client = axios.create({
  baseURL: "http://localhost:8080/api/v1",
  headers: { "Access-Control-Allow-Origin": "*" },
});

export default client;
```

### Executar a aplicação

Execute o ambiente local de desenvolvimento com o comando abaixo:

```shell copy filename="Terminal"
npm run dev
```

</Steps>

<Callout emoji="💡">
  Você pode parar ambiente local de desenvolvimento fechando o terminal ou
  pressionando `CTRL + C` no Windows e Linux, ou `⌘ + .` no macOS.
</Callout>
